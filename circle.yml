dependencies:
  pre:
    - gem install bundler
  post:
    - bundle exec middleman contentful --rebuild
    - bundle exec middleman build
    - bundle exec middleman s3_sync
test:
  override:
    - "true"

