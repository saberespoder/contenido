dependencies:
  pre:
    - gem install bundler
  post:
    - bundle exec middleman contentful
    - bundle exec middleman build
    - " [ $CIRCLE_BRANCH != master ] || bundle exec middleman s3_sync "

test:
  override:
    - bundle exec rspec spec:
        environment:
          DATA_DIR: sample
