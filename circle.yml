dependencies:
  pre:
    - gem install bundler
  post:
    - bundle exec middleman contentful --rebuild
    - bundle exec middleman build
    - if [ $CIRCLE_BRANCH = master ]; then bundle exec middleman s3_sync; fi
test:
  override:
    - "true"

